name: LightGBMServer Docker Publisher

on:
  push:
    branches: [master]
    tags: [v*]
  pull_request:

jobs:
  publish:
    uses: ./.github/workflows/docker-publish.yml
    with:
      image-name: lgbserver
      context: python
      dockerfile: python/lgb.Dockerfile
    secrets: inherit
