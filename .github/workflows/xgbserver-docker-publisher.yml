name: Xgbserver Server Docker Publisher

on:
  push:
    branches: [master]
    tags: [v*]
  pull_request:

jobs:
  publish:
    uses: ./.github/workflows/docker-publish.yml
    with:
      image-name: xgbserver
      context: python
      dockerfile: python/xgb.Dockerfile
    secrets: inherit
