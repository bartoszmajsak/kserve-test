name: ARTExplainer Docker Publisher

on:
  push:
    branches: [master]
    tags: [v*]
  pull_request:

jobs:
  publish:
    uses: ./.github/workflows/docker-publish.yml
    with:
      image-name: art-explainer
      context: python
      dockerfile: python/artexplainer.Dockerfile
    secrets: inherit
