name: Sklearn Server Docker Publisher

on:
  push:
    branches: [master]
    tags: [v*]
  pull_request:

jobs:
  publish:
    uses: ./.github/workflows/docker-publish.yml
    with:
      image-name: sklearnserver
      context: python
      dockerfile: python/sklearn.Dockerfile
    secrets: inherit
